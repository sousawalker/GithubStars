import { TestBed, inject } from '@angular/core/testing';

import { StarsPaginationService } from './stars-pagination.service';

describe('StarsPaginationService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [StarsPaginationService]
    });
  });
});
